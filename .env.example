# Execution API (optional). When set, "Run Tests" uses real Rust compilation and execution.
# Start the server with: bun run server
VITE_EXECUTION_API_URL=http://localhost:3847

# Port for the execution server (default: 3847)
EXECUTION_PORT=3847

# CORS origin for the execution API. In production set to your app origin (e.g. https://yourapp.com). Unset = "*" (dev).
# CORS_ORIGIN=https://yourapp.com
